# I1 - Reword and drop  (Easy)

`seed/i1` has three commits on top of `main`. Clean them up with a single
interactive rebase, producing `answer/i1`:

- `add lables route`   -> reword to `Add labels route` (typo fixed)
- `temp debug output`  -> drop it entirely; `debug.log` must not exist in the
                          final tree and must not appear in any commit that
                          remains
- `Fetch labels from the client` -> leave alone

`answer/i1` must have exactly TWO commits on top of `main`.

In SOLUTIONS.md, paste your final todo list (the lines you left in the
editor) and name the command that opens the rebase for the last 3 commits.
