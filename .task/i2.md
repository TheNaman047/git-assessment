# I2 - Squash and fixup  (Medium)

`seed/i2` has four commits on top of `main`:

    Add comments route
    also export the array
    Add comments test
    fixup! Add comments test

Produce `answer/i2` with exactly TWO commits on top of `main`:

1. `Add comments route` - absorbing `also export the array`, and the combined
   commit's message must be exactly `Add comments route`, with no trace of
   the second subject in the message body.
2. `Add comments test` - absorbing `fixup! Add comments test`, message
   exactly `Add comments test`.

Both final trees must match `seed/i2`'s tip content for the files involved.

In SOLUTIONS.md: explain the practical difference between the `squash` and
`fixup` todo keywords, and name the flag that would have let git pair the
`fixup!` commit with its target automatically.
