# I3 - Rearrange and split  (Hard)

`seed/i3` has three commits on top of `main`. The middle one does two
unrelated things at once, and the docs commits are separated for no reason.

Produce `answer/i3` with exactly FOUR commits on top of `main`, in exactly
this order and with exactly these subjects:

1. `Add archive route`            - touches only `api/routes/tasks.js`. Adds
                                     the `archived` array, a `routeArchive`
                                     function that writes its own headers the
                                     long way, and the export. It must NOT
                                     introduce the `json()` helper.
2. `Tidy the task route`          - touches only `api/routes/tasks.js`. This
                                     is the other half of the original middle
                                     commit: extract the shared `json(res,
                                     body)` helper and route both handlers
                                     through it.
3. `docs: document GET /tasks`    - touches only `docs/API.md`.
4. `docs: document GET /archive`  - touches only `docs/API.md`.

The final tree of `answer/i3` must be identical to the final tree of
`seed/i3`.

In SOLUTIONS.md: name the todo keyword that lets you stop mid-rebase to
split a commit, and list the commands you ran at that stop.
