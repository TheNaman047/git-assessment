# M3 - Rebase a feature branch through a conflict  (Hard)

`seed/m3-feature` has three commits taken from an older point on
`seed/m3-main`. Main has since moved on and touched `shared/format.js` too.

Produce `answer/m3`: `seed/m3-feature`'s three commits replayed on top of
`seed/m3-main`, in their original order, with no merge commit anywhere.

The conflict is in `shared/format.js`. Resolve it so the final file is
exactly:

    function formatTask(t) {
      return `[${t.status.toUpperCase()}] ${t.title}`;
    }

    function formatList(tasks) {
      return tasks.map(formatTask).join('\n');
    }

    module.exports = { formatTask, formatList };

In SOLUTIONS.md: name the command that aborts a rebase gone wrong, the one
that continues it after you stage a resolution, and say in one sentence why
`answer/m3`'s commit SHAs differ from `seed/m3-feature`'s even for the
commits that had no conflict.
