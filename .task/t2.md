# T2 - A tag pointing at the wrong commit  (Medium)

The annotated tag `v2.0.0-t2` was cut one commit too late: it points at
`WIP: start PUT support (broken)` when it should point at
`Harden the task route`.

Fix it, in your fork:

1. Move `v2.0.0-t2` so it points at `Harden the task route`. It must stay an
   **annotated** tag and its message must stay exactly `TaskFlow 2.0.0`.
2. Make your fork's remote reflect the move - after you are done,
   `git ls-remote --tags` on a fresh clone must show the corrected target,
   not the old one.
3. Additionally create an annotated tag `v2.0.1-t2` with message
   `TaskFlow 2.0.1` on the tip of `seed/t2`, and push it.

In SOLUTIONS.md, explain in two sentences why moving an already-published
tag is considered dangerous, and what a colleague who already fetched the
old tag has to do.
