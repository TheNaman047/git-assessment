# V2 - Three scruffy commits, one clean one  (Medium)

`seed/v2` carries three commits - `wip`, `wip again`, `fix typo` - that were
never pushed anywhere. Collapse them.

`answer/v2` must:

- contain exactly ONE commit on top of `main`;
- have that commit's message be exactly `Add bearer token parsing`;
- produce byte-for-byte the same `api/auth.js` as `seed/v2`'s tip.

Do it with a reset, not with an interactive rebase - and say in SOLUTIONS.md
which reset mode you used and what the other two modes would have done to
your index and working tree.
