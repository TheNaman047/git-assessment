# V3 - Undo a whole merged feature  (Hard)

The `webhooks` feature was merged into `seed/v3` and turned out to be a bad
idea. It has to come out, but the merge is already public.

On `answer/v3` (from `seed/v3`), undo the entire merge in one new commit.
Afterwards:

- `api/webhooks.js` must not exist;
- `api/server.js` must be back to its pre-merge content (no `notify` import,
  no `notify(req.url)` call);
- `docs/RUNBOOK.md` must still contain the "Logs live in" line;
- the merge commit must still be present in `git log`;
- `answer/v3` must have exactly one commit more than `seed/v3`.

In SOLUTIONS.md: give the exact command including the flag that makes this
possible, explain what the number you passed to that flag selects, and state
what goes wrong later if the team ever wants to merge `webhooks` again.
