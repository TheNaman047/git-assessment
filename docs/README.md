# TaskFlow

A tiny task tracker used as the sandbox for the Git assessment.

    api/      HTTP layer
    web/      browser client
    shared/   code used by both
    docs/     documentation

Run `npm start` to boot the API on port 3000.
